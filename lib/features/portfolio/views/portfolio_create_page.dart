import 'package:flutter/material.dart';

class PortfolioCreatePage extends StatelessWidget {
  const PortfolioCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Portfolio Create')));
  }
}
